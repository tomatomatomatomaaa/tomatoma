#include <iostream>

int main() {
    int x;
    std::cout << "Введите число: ";
    std::cin >> x;

    bool isEven = (x % 2 == 0);

    std::cout << "Число " << x << " является четным: " << (isEven ? "Да" : "Нет") << std::endl;

    return 0;
}
