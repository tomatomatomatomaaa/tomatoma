#include <cstdlib>
#include <iostream>

bool isPowerOfTwo(unsigned int n) {
return n && (!(n & (n - 1)));
}

int main() {
unsigned int n;
std::cin >> n;
if (isPowerOfTwo(n)) {
std::cout << n << " is a power of 2.\n";
} else {
std::cout << n << " is not a power of 2.\n";
}
}
