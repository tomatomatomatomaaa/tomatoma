#include <iostream>

int main() {
    int x;
    std::cout << "Введите число: ";
    std::cin >> x;

    bool powerOf2 = (x & (x - 1)) == 0;

    std::cout << "Число " << x << " является степенью двойки: " << (powerOf2 ? "Да" : "Нет") << std::endl;

    return 0;
}

