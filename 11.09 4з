#include <iostream>

#define cubeof(X) ((X)*(X)*(X))

int main() {
    int num = 10;
    int result = cubeof(num);
    std::cout << "Cube of " << num << " is " << result << std::endl;
    return 0;
}
